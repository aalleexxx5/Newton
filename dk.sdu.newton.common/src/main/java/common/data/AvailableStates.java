package common.data;

/**
 * Enum over the gameStates.
 * A game state entails a completely different set of objects to be drawn to the screen.
 */
public enum AvailableStates{
	PLAY_STATE,
	PAUSED_STATE;
}
